import { CurrenyCustomPipe } from './curreny-custom.pipe';

describe('CurrenyCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenyCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
